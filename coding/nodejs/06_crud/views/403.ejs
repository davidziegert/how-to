<%- include("./partials/head.ejs"); %> <%- include("./partials/header.ejs"); %> <%- include("./partials/nav.ejs"); %>

<main>
  <div class="row">
    <div class="column">
      <h1><%= txt_title %></h1>
      <p>The <strong><%= txt_title %></strong> indicates that the server understood the request but refuses to authorize it. A server that wishes to make public why the request has been forbidden can describe that reason in the response payload (if any).</p>
    </div>
  </div>
</main>

<%- include("./partials/footer.ejs"); %>
